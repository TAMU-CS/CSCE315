
int MAX = 1000;
int MIN = -1000;

public int minimax(int depth, int nodeIndex, boolean maxPlayer,
                    int board[], int alpha, int beta, int playerTurn) {
  // Reached leaf
  if(depth == 3) {
    return board
  }
}
